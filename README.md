# TypeScript Pokedex

A REPL console app the simulates the usage of a Pokedex, making use of pokeAPI, using caching in memory to serve information already fetched back to the user without unnecessary extra requests.
